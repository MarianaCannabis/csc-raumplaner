# P10.2 — Code Cleanup Report

Stand: 2026-04-20

## Broken-Flows aus P10.1

| Flow | Location | Fix |
|---|---|---|
| `updOP(...)` → undefined | index.html:8474-8476 (3×, Folien-Farb-Handler) | → `updateObjProp(...)` |
| `rechtsklickRaum(rooms.find(...))` | index.html:22900 (🪑 Möbel auto-anordnen) | → `autoArrangeFurniture('<roomId>')` |
| `try{…}catch{}` inline | index.html:4012 (Cookie-Banner) | False-Positive (inline try/catch, keine Funktion) |

Ergebnis: **728 resolved / 1 unresolved** (der verbleibende Eintrag ist ein Regex-False-Positive des Detectors, keine echte Lücke).

## Dead-Code-Elimination (ts-prune)

Vor dem Cleanup listete `ts-prune` 7 wirklich ungenutzte Exports (alle anderen Treffer sind `(used in module)` = intern über Re-Exports verwendet).

Entfernt aus `src/three/materials.ts`:
- `woodMaterial`, `metalMaterial`, `fabricMaterial`, `plasticMaterial`, `glassMaterial`, `concreteMaterial` (6 Named-Factory-Aliase — `makeMaterial(kind, color)` ist die eigentliche API, die Aliase wurden nirgendwo importiert)

Behalten:
- `disposeImageMapTexture` — Counter­part zu `imageMapMaterial`, wird beim Entfernen von Bild-Uploads gebraucht (Referenz bleibt für zukünftige Aufräum-Calls im Legacy-JS).

## Duplicate-Code (jscpd, src/)

`Found 0 exact clones with 0(0%) duplicated lines in 63 files.` — keine Dubletten im TypeScript-Code-Bestand.

Der Legacy-`index.html`-Block (~21k Zeilen) enthält historisch Helper-Dubletten, die als Teil der Strangler-Migration inkrementell nach `src/` wandern.

## Unused Dependencies (depcheck)

`{"dependencies":[],"devDependencies":[],"missing":{}}` — alle 8 Dev- und Prod-Packages sind in Gebrauch.

## TypeScript Strict-Mode

`tsconfig.json` hat jetzt zusätzlich:
- `noFallthroughCasesInSwitch: true`
- `noImplicitReturns: true`

Bereits aktiv: `strict`, `noUncheckedIndexedAccess`, `noImplicitOverride`, `isolatedModules`, `noEmit`.

`npx tsc --noEmit` läuft fehlerfrei durch, `npm test` grün (20/20).

## CSS-Audit

Alle Styles liegen heute inline in `index.html` (`<style>`-Blöcke) — ein externer `purgecss`-Run würde hier keine greifbaren Wins liefern, solange `index.html` nicht in HTML+CSS+JS gesplittet ist. Splitting ist ein v2.1-Task (siehe `docs/BUNDLE-SIZE.md`).

## Bundle-Size nach Cleanup

| Asset | raw | gz |
|---|---|---|
| `index.html` | 1,377 KB | **362 KB** |
| `three.module` (main) | 717 KB | 183 KB |
| `index.js` | 255 KB | 69 KB |
| `GLTFLoader` | 44 KB | 13 KB |
| `three.module` (chunk) | 14 KB | 5 KB |
| `escapeRoute.worker` | 3 KB | <1 KB |
| **Total gz** | — | **~633 KB** |

Ziel <400 KB gz bleibt für v2.1 (index.html-Split + Lazy-3D-Chunk).

## SLOC-Snapshot

- `index.html`: 32,170 Zeilen (Legacy-Monolith, enthält HTML + inline CSS + inline JS)
- `src/**/*.ts`: 6,693 Zeilen (migrierte Module)
- Ratio src/html: ~21 % — Strangler-Fortschritt.

## Quality-Gates (post-P10.2)

- ✅ `npm test` — 20/20 green
- ✅ `npx tsc --noEmit` — clean
- ✅ `npm run audit:broken-flows` — 728/729 resolved (1 false-positive)
- ✅ `npm run build` — success
- ✅ `npx depcheck` — 0 unused
- ✅ `npx jscpd src` — 0 duplicates
